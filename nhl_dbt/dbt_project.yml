name: 'nhl_dbt'
version: '1.0.0'

profile: 'nhl_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  nhl_dbt:
    staging:
      +materialized: view
      +schema: staging
    gold:
      +materialized: table
      +schema: gold